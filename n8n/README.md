# n8n Workflow

Workflow:
- finance-intelligence-workflow.json

Requirements:
- n8n v2.x
- FastAPI backend running on http://host.docker.internal:8000
- Airtable credentials configured
- Slack credentials configured
- Email credentials configured

Import:
Workflow → Import from File